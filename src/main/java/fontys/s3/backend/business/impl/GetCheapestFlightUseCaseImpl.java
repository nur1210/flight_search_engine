package fontys.s3.backend.business.impl;

/*@Service
@AllArgsConstructor
public class GetCheapestFlightUseCaseImpl implements GetCheapestFlightUseCase {

    private FlightRepository flightRepository;
    @Override
    public GetCheapestFlightResponse getCheapestFlight(GetCheapestFlightRequest request) {
        FlightEntity result = flightRepository.FindTop3CheapestFlightFromOrigin(request.getOriginCode());

        final GetCheapestFlightResponse response = new GetCheapestFlightResponse();
        Flight flight = FlightConverter.convert(result);
        response.setFlight(flight);

        return response;
    }
}*/
